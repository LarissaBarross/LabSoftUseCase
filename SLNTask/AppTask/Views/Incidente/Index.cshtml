@model IEnumerable<AppTask.Models.Incidente>

@{
    ViewData["Title"] = "Index";
}

<h1 class="mb-4">Lista de Incidentes</h1>

<p>
    <a href="/Incidente/Create" class="btn btn-success text-white">Novo</a>
</p>

<table class="table table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th>Descrição do Problema</th>
            <th>Data do Incidente</th>
            <th>Solução</th>
            <th>Resolvido</th>
            <th>Ações</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.DescricaoProblema</td>
                <td>@item.DataIncidente</td>
                <td>@item.Solucao</td>
                <td>@item.Resolvido</td>

                <td>
                    <a href="/Incidente/Edit/@item.Codigo"
                       class="btn btn-primary btn-sm text-white">
                        Alterar
                    </a>

                    <a href="/Incidente/Details/@item.Codigo"
                       class="btn btn-success btn-sm text-white"
                       style="background-color: #006400;">
                        Detalhe
                    </a>

                    <button type="button"
                            class="btn btn-danger btn-sm text-white"
                            data-bs-toggle="modal"
                            data-bs-target="#deleteModal"
                            data-id="@item.Codigo"
                            data-descricao="@item.DescricaoProblema"
                            data-action="/Incidente/Delete/@item.Codigo">
                        Delete
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Modal de Confirmação de Exclusão -->
<div class="modal fade"
     id="deleteModal"
     tabindex="-1"
     aria-labelledby="deleteModalLabel"
     aria-hidden="true">

    <div class="modal-dialog">
        <div class="modal-content">

            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteModalLabel">
                    Confirmação de Exclusão
                </h5>

                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="modal"
                        aria-label="Close">
                </button>
            </div>

            <div class="modal-body">
                Deseja realmente excluir o incidente:
                <strong id="incidenteDescricao"></strong>?
            </div>

            <div class="modal-footer">

                <button type="button"
                        class="btn btn-secondary"
                        data-bs-dismiss="modal">
                    Cancelar
                </button>

                <form id="deleteForm" method="post" action="">
                    @Html.AntiForgeryToken()

                    <button type="submit" class="btn btn-danger">
                        Sim, Excluir
                    </button>
                </form>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        var deleteModal = document.getElementById('deleteModal');

        deleteModal.addEventListener('show.bs.modal', function (event) {

            var button = event.relatedTarget;

            var descricao =
                button.getAttribute('data-descricao');

            var action =
                button.getAttribute('data-action');

            var modalDescricaoSpan =
                deleteModal.querySelector('#incidenteDescricao');

            modalDescricaoSpan.textContent = descricao;

            var deleteForm =
                deleteModal.querySelector('#deleteForm');

            deleteForm.action = action;
        });
    </script>
}
